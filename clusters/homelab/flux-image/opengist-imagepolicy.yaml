apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: opengist
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: opengist
  policy:
    semver:
      range: ">=1.13.0"
