apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: skate-streamer-receiver
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: skate-streamer-receiver
  filterTags:
    pattern: '^v\d+\.\d+\.\d+$'
  policy:
    semver:
      range: ">=1.0.0-0"
