apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: helpdesk
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: helpdesk
  policy:
    semver:
      range: ">=1.0.0"
