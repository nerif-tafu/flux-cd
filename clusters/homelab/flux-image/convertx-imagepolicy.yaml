# Prefer :latest (alphabetical asc picks it when repo has latest, main, pr-*).
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: convertx
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: convertx
  policy:
    alphabetical:
      order: asc
