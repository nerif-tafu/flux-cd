apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: rust-plus-provider
  namespace: flux-system
spec:
  image: ghcr.io/nerif-tafu/rust-plus-provider
  interval: 5m49s
  secretRef:
    name: ghcr-registry-secret
