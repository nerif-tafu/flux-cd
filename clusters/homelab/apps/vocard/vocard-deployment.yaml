apiVersion: apps/v1
kind: Deployment
metadata:
  name: vocard
  namespace: vocard
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: vocard
  template:
    metadata:
      labels:
        app: vocard
    spec:
      initContainers:
        - name: wait-db
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z vocard-db 27017; do echo waiting for mongo; sleep 2; done"]
        - name: copy-app-and-settings
          image: ghcr.io/chocomeow/vocard:beta@sha256:474642880b784c88448dc6cc0c36f6a843c921a67c67fdb9345b90da8d8ba815 # {"$imagepolicy": "flux-system:vocard"}
          command: ["sh", "-c", "cp -r /app/. /app-writable/ && cp /app-secret/settings.json /app-writable/settings.json"]
          volumeMounts:
            - name: app-data
              mountPath: /app-writable
            - name: settings
              mountPath: /app-secret
              readOnly: true
      containers:
        - name: vocard
          image: ghcr.io/chocomeow/vocard:beta@sha256:474642880b784c88448dc6cc0c36f6a843c921a67c67fdb9345b90da8d8ba815 # {"$imagepolicy": "flux-system:vocard"}
          resources:
            requests:
              cpu: 20m
              memory: 128Mi
            limits:
              memory: 512Mi
          volumeMounts:
            - name: app-data
              mountPath: /app
      volumes:
        - name: app-data
          emptyDir: {}
        - name: settings
          secret:
            secretName: vocard-settings
