apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: factorio
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: factorio
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: ">=2.0.0 <2.1.0"
