apiVersion: apps/v1
kind: Deployment
metadata:
  name: pipe-dreams
  namespace: pipe-dreams
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: pipe-dreams
  template:
    metadata:
      labels:
        app: pipe-dreams
    spec:
      containers:
        - name: pipe-dreams
          image: ghcr.io/nerif-tafu/pipe-dreams:v1.0.7 # {"$imagepolicy": "flux-system:pipe-dreams"}
          env:
            # Keep the downloaded item data and images on the mounted volume.
            # Without this they are written into the image layer, so every
            # image update silently empties the item list until someone runs
            # a refresh again.
            - name: DATA_DIR
              value: /data
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: pipe-dreams-data
