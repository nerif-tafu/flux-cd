apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: rust-booter
  namespace: flux-system
spec:
  image: ghcr.io/nerif-tafu/rust-booter
  interval: 5m37s
  secretRef:
    name: ghcr-registry-secret
