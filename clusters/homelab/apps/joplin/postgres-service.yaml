apiVersion: v1
kind: Service
metadata:
  name: joplin-postgres
  namespace: joplin
spec:
  type: ClusterIP
  selector:
    app: joplin-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
