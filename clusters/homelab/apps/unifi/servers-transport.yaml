# Traefik → UniFi uses the controller's self-signed cert on 8443.
apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: unifi-insecure
  namespace: unifi
  labels:
    traefik.tafu.casa/instance: internal
spec:
  insecureSkipVerify: true
  disableHTTP2: true
