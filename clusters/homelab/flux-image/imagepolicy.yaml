apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: shorturl
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: shorturl
  policy:
    semver:
      range: ">=1.0.0"
