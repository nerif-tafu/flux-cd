apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: vocard
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: vocard
  policy:
    alphabetical:
      order: desc
