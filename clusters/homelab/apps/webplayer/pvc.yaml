apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webplayer-data
  namespace: webplayer
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200Gi
