apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: proxmox
  namespace: proxmox
spec:
  secretName: proxmox-cl-tafu-casa-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - proxmox.cl.tafu.casa
