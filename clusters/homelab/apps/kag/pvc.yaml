apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kag-data
  namespace: kag
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
