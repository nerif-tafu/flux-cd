apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  # BlueMap: renders the world and serves it at mc.tafu.casa over HTTPS.
  - bluemap-pvc.yaml
  - bluemap-deployment.yaml
  - bluemap-service.yaml
  - bluemap-ingress.yaml
