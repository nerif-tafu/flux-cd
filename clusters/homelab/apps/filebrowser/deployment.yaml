# FileBrowser Quantum: persist config/db/cache on PVC at /home/filebrowser/data, files at /srv.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: filebrowser
  namespace: filebrowser
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: filebrowser
  template:
    metadata:
      labels:
        app: filebrowser
    spec:
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
      initContainers:
        - name: init-data-dirs
          image: busybox:1.36
          securityContext:
            runAsUser: 0
          command:
            - sh
            - -ec
            - |
              mkdir -p /data/tmp
              chown -R 1000:1000 /data
              # Not -R: /srv is now the NAS share, and a recursive chown over
              # NFS walks every file on every pod start. The migrated tree
              # already carries the right ownership, and filebrowser creates
              # anything new as itself.
              chown 1000:1000 /srv
          volumeMounts:
            - name: data
              mountPath: /data
            - name: files
              mountPath: /srv
      containers:
        - name: filebrowser
          image: ghcr.io/gtsteffaniak/filebrowser:1.5.4-stable # {"$imagepolicy": "flux-system:filebrowser"}
          resources:
            requests:
              cpu: 10m
              memory: 128Mi
            limits:
              memory: 512Mi
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 120
            periodSeconds: 30
            failureThreshold: 6
          env:
            - name: FILEBROWSER_CONFIG
              value: /home/filebrowser/data/config.yaml
            - name: FILEBROWSER_DATABASE
              value: /home/filebrowser/data/database.db
            - name: FILEBROWSER_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: filebrowser-secret
                  key: FILEBROWSER_ADMIN_PASSWORD
          ports:
            - containerPort: 80
          volumeMounts:
            - name: data
              mountPath: /home/filebrowser/data
            - name: config
              mountPath: /home/filebrowser/data/config.yaml
              subPath: config.yaml
            # The served files, on the NAS since 2026-08-12. Previously this was
            # subPath `files` of the Longhorn claim; that claim now holds only
            # database.db and config.yaml. See pvc-files.yaml.
            - name: files
              mountPath: /srv
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: filebrowser-data
        - name: files
          persistentVolumeClaim:
            claimName: filebrowser-files
        - name: config
          configMap:
            name: filebrowser-config
