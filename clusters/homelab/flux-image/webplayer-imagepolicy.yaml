apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: webplayer
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: webplayer
  policy:
    semver:
      range: ">=1.0.0"
