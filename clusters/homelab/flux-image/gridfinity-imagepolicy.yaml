apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: gridfinity
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: gridfinity
  policy:
    semver:
      range: ">=1.0.0"
