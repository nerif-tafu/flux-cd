apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: fitv2
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: fitv2
  policy:
    semver:
      range: ">=1.0.0"
