apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: rust-booter
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: rust-booter
  policy:
    semver:
      range: ">=1.0.0"
