apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - pvc.yaml
  - pvc-dynmap.yaml
  - rcon-secret.yaml
  - deployment.yaml
  - service.yaml
  # Dynmap: a Forge mod inside the server, serving its web UI at mc.tafu.casa.
  # Replaced BlueMap, which cannot render 1.12.2 in 3D — see the deployment.
  - dynmap-ingress.yaml
